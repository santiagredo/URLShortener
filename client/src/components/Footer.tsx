
export function Footer () {
    const footerPClasses = "w-full h-20 flex items-center justify-center text-center";

    return (
        <header className={footerPClasses}>
            <p>All URLs will be automatically deleted after 24 hours.</p>
        </header>
    );
};